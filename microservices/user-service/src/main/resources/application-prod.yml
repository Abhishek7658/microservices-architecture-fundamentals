server : 
    port: 8092
spring:
    datasource:
        url: jdbc:mysql://localhost:3306/user_service_prod_db
        username: ${DB_USERNAME}
        password: ${DB_PASSWORD}
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        